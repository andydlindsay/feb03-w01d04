const sayHello = function(name) {
  console.log(`hello there ${name}`); // whatever we pass in
};

const sayHello = name => {
  console.log(`hello there ${name}`); // whatever we pass in
};

function() {}
() => {}

// this
